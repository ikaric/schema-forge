# Target spec

No spec yet. `/target` writes the machine-checkable target spec here as a fenced
`json` block — the assertions that make `verified` mean something. See
`examples/overdrive/spec.md` for the format.
